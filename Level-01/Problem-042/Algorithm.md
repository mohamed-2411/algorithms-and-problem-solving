# Algorithm

1. **Start**
2. **Read** `Days`, `Hours`, `Minutes`, `Seconds`
3. Calculate `TotalSeconds = (Days × 24 × 60 × 60) + (Hours × 60 × 60) + (Minutes × 60) + Seconds`
4. **Print** `TotalSeconds + " Seconds"`
5. **End**
